name : "website"

collections:
  tag :
    output: true
    permalink : /tag/:path

defaults :
  - scope:
      path: ""
      type : "posts"
    values:
      layout: "post"
      category: "post"
  - scope:
      path: ""
      type: "tag"
    values:
      layout: "timeline"

highlighter: rouge

kramdown:
  syntax_highlighter: rouge
  input: GFM

ARRAY_EMPTY : []
