{%- assign the_post_index_list =  site.ARRAY_EMPTY -%}

{%- assign array = include.from_array_of_tags | uniq -%}

{%- for tag in include.from_array_of_tags -%}

    {%- comment -%} inner_post_index_list {%- endcomment -%}
    {%- include as_html_the_post_index_list_from_tag.liquid from_tag=tag -%}
    {% comment %} result is in the return {% endcomment %}
    {%- comment -%} `Liquid` does not have functions {%- endcomment -%}

    {%- assign the_post_index_list = the_post_index_list | push : return -%}

{%- endfor -%}

{%- assign return = the_post_index_list | uniq | compact -%}