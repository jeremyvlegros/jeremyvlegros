{%- assign _input = include.from | upcase -%}
{%- case _input -%}
    {%- when "" or "NULL" or null -%}
        {{ null }}
    {%- else -%}
        {{ include.from }}
{%- endcase -%}
{%- assign _input = null -%}