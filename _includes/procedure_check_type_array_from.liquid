{%- assign _test_array = "1,2" | split : "," -%}

{%- comment -%} will crash if is not an array {%- endcomment -%}
{%- assign _test_array_1 = _test_array | concat : include.from -%}
{%- assign _test_array_2 = include.from | concat : _test_array -%}
{%- comment -%} {%- assign _test_array = site.ARRAY_EMPTY | concat : include.from -%} {%- endcomment -%}