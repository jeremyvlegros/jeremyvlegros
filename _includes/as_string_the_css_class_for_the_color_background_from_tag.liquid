{%- assign prefix = "color_background_" -%}

{%- comment -%}  {%- endcomment -%}
{%- include as_string_the_word_from_tag.liquid from_tag=include.from_tag -%}
{%- assign word = return -%}
{%- assign return = null -%}
{%- comment -%} `Liquid` does not have functions {%- endcomment -%}

{%- assign return = prefix | append : word -%}
