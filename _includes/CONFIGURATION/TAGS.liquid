{%- include CONFIGURATION/ARRAY_EMPTY.liquid -%}

{% comment %}
I thought I could optimize things, but I can't declare global variables in liquid code.
What a shame.
{% endcomment %}

{% comment %}<!-- TAGS BEFORE INIT : `{{ TAGS | JSONIFY }}` !-->{% endcomment %}

{%- if TAGS == null -%}

  {%- assign TAGS = ARRAY_EMPTY -%}

  {%- for tag in site.tags -%}

    {%- assign TAGS = TAGS | push : tag[0]-%}

  {%- endfor -%}

{% comment %}<!-- TAGS AFTER INIT : `{{ TAGS | JSONIFY }}` !-->{% endcomment %}
  
{%- endif -%}

