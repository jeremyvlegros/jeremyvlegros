{%- comment -%} as_canonical_the_page_url.liquid {%- endcomment -%}
{%- assign _site_name = site.name -%}

{%- capture _page_url -%}
    {%- include as_canonical_url_from_url.liquid from_url=page.url -%}
{%- endcapture -%}

{%- comment -%} return {%- endcomment -%}
{%- capture _return -%}
    {%- include return_from.liquid from=_page_url-%}
{%- endcapture -%}
{{ _return }}
{%- assign _return = null -%}

{%- assign _site_name = null -%}
{%- assign _page_url = null -%}

{%- comment -%} {%- endcomment -%}